<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bahb_background">

    <!-- DJI FPV Widget - Full screen background -->
    <dji.v5.ux.cameracore.widget.fpvinteraction.FPVInteractionWidget
        android:id="@+id/fpv_widget"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- BAHB Detection Overlay - On top of FPV -->
    <com.bahb.ui.overlay.DetectionOverlayView
        android:id="@+id/detection_overlay"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Top Status Bar -->
    <LinearLayout
        android:id="@+id/status_bar"
        android:layout_width="0dp"
        android:layout_height="48dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="16dp"
        android:background="@color/panel_background"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Connection Status Indicator -->
        <View
            android:id="@+id/status_indicator"
            android:layout_width="12dp"
            android:layout_height="12dp"
            android:background="@drawable/circle_indicator" />

        <TextView
            android:id="@+id/status_manifold"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="@string/manifold_disconnected"
            android:textColor="@color/text_secondary"
            android:textSize="14sp" />

        <View
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1" />

        <!-- Inspection Status -->
        <TextView
            android:id="@+id/status_inspection"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/status_idle"
            android:textColor="@color/bahb_accent"
            android:textSize="16sp"
            android:textStyle="bold" />

        <View
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1" />

        <!-- System Stats -->
        <TextView
            android:id="@+id/stats_fps"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="-- FPS"
            android:textColor="@color/text_secondary"
            android:textSize="14sp"
            android:layout_marginEnd="16dp" />

        <TextView
            android:id="@+id/stats_gpu"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="--% GPU"
            android:textColor="@color/text_secondary"
            android:textSize="14sp" />

    </LinearLayout>

    <!-- Left Side Control Panel -->
    <LinearLayout
        android:id="@+id/control_panel"
        android:layout_width="72dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="8dp"
        android:background="@color/panel_background"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/status_bar"
        app:layout_constraintBottom_toTopOf="@id/bottom_bar">

        <!-- Start/Stop Inspection -->
        <Button
            android:id="@+id/btn_inspection"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:text="▶"
            android:textSize="24sp"
            android:backgroundTint="@color/bahb_primary"
            android:layout_marginBottom="8dp" />

        <!-- Capture Snapshot -->
        <Button
            android:id="@+id/btn_capture"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:text="📷"
            android:textSize="20sp"
            android:backgroundTint="@color/bahb_primary_dark"
            android:layout_marginBottom="8dp" />

        <!-- Thermal Toggle -->
        <Button
            android:id="@+id/btn_thermal"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:text="🌡"
            android:textSize="20sp"
            android:backgroundTint="@color/thermal_warm"
            android:layout_marginBottom="8dp" />

        <!-- Anomaly Panel Toggle -->
        <FrameLayout
            android:layout_width="56dp"
            android:layout_height="56dp">

            <Button
                android:id="@+id/btn_anomalies"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:text="⚠"
                android:textSize="20sp"
                android:backgroundTint="@color/severity_medium" />

            <!-- Anomaly Count Badge -->
            <TextView
                android:id="@+id/anomaly_badge"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_gravity="top|end"
                android:gravity="center"
                android:text="0"
                android:textColor="@color/text_primary"
                android:textSize="12sp"
                android:textStyle="bold"
                android:background="@drawable/badge_circle"
                android:visibility="gone" />
        </FrameLayout>

    </LinearLayout>

    <!-- Right Side Anomaly Panel (Collapsible) -->
    <LinearLayout
        android:id="@+id/anomaly_panel"
        android:layout_width="280dp"
        android:layout_height="0dp"
        android:orientation="vertical"
        android:padding="12dp"
        android:background="@color/panel_background"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/status_bar"
        app:layout_constraintBottom_toTopOf="@id/bottom_bar">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/panel_anomalies"
            android:textColor="@color/text_primary"
            android:textSize="18sp"
            android:textStyle="bold"
            android:layout_marginBottom="8dp" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/anomaly_list"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />

    </LinearLayout>

    <!-- Bottom Info Bar -->
    <LinearLayout
        android:id="@+id/bottom_bar"
        android:layout_width="0dp"
        android:layout_height="40dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="16dp"
        android:background="@color/panel_background"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Detection Count -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Detections: "
            android:textColor="@color/text_secondary"
            android:textSize="14sp" />

        <TextView
            android:id="@+id/detection_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0"
            android:textColor="@color/bahb_accent"
            android:textSize="14sp"
            android:textStyle="bold" />

        <View
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1" />

        <!-- DJI Flight Status Widgets -->
        <dji.v5.ux.core.widget.battery.BatteryWidget
            android:id="@+id/battery_widget"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="16dp" />

        <dji.v5.ux.core.widget.gpssignal.GPSSignalWidget
            android:id="@+id/gps_widget"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="16dp" />

        <dji.v5.ux.core.widget.remotecontrollersignal.RemoteControllerSignalWidget
            android:id="@+id/rc_signal_widget"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
