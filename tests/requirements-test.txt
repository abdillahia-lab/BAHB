# Test Requirements for BAHB
# Install with: pip install -r requirements-test.txt

# Core testing framework
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.1
pytest-asyncio>=0.21.1
pytest-timeout>=2.1.0

# Performance testing
pytest-benchmark>=4.0.0

# Additional utilities
psutil>=5.9.5  # For memory monitoring
pyyaml>=6.0    # For YAML configs

# Parent package requirements (if not already installed)
-r ../requirements.txt
